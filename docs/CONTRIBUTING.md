# Contributing Guide (HoroloGen)

## 1. 目的
HoroloGen は「正規時計店向けの商品説明文生成」を安全に運用するためのアプリです。  
このガイドは、変更時に壊してはいけないポイントと、作業ルールを簡潔にまとめます。

---

## 2. 変更の前に読む（最重要）
以下は **絶対に壊さないこと**。

- **事実優先順位**：canonical_specs > remarks > reference本文  
- **specs_textは canonical_specs の項目だけ**（テンプレ順序固定、装飾・評価禁止）
- **editor_note は intro_text に必ず反映**
- **語り手は正規店スタッフ固定**
- **言い換えは最大1回**（UIだけでなくサーバ側ガード必須）
- **月間クォータ**：LLM呼び出し前にブロック、DBトランザクションで安全に更新
- **エラー表示**：ユーザー向け文言に差し替え、詳細はログに残す
-in/upload` が開く
- `/staff/searく
- 検索 �ical表示ができる
- オーバーライド保存/解除ができる

### 6.2 生成
- limited時）

### 6.3 言い換え
- 言い換え1回目が成功（depth=1, parent設定）
- 同じ親から2回目がブロックされる（サーバ側）
- 言い換え済み履歴からの言い換えがブロックされる

### 6.4 クォータ
- 上限到達時に生成・言い換えがブロックされる
- unlimited時はブロックされない

### 6.5 エラー差し替え
- APIキー無し/クレジット不足/レート制限/timeout を人間向け文言に差し替え
- ログに出
  - 最終保険で tools無し JSON-only 再試行

### 7.3 生成結果表示中のフォーム変更事故
- 対応：生成結果があるときはトーン/チェック/URL を disabled（UIロック）

---

## 8. 新機能追加のときの作法
### 8.1 DB変更
- `init_db()` に追加する（存在しない場合のみALTERするのが基本）
- テーブル変更は「後方互換」優先（既存dbが壊れないこと）

### 8.2 テンプレ変更
- 生成フォーム周辺は事故が起きやすいので、結果表示時の状態（disabledや注意文）を必ず確�ファイル/Cloud）
- バックアップ運用（db, uploads）

---
